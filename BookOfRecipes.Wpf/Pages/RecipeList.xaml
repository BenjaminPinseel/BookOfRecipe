<Page x:Class="BookOfRecipes.Wpf.RecipeList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookOfRecipes.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RecipeList">

    <StackPanel Height="925" Orientation="Horizontal">
        <ListView x:Name="lstRecipeList" HorizontalAlignment="Left" Width="240" Height="925" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Naam" Width="120" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Prijs" Width="120" DisplayMemberBinding="{Binding PriceRange}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Width="562" Height="925">
            <Label FontSize="25" HorizontalAlignment="Center">Ingredienten:</Label>
            <Label>Koolhydraten:</Label>
            <Label>Eiwitten</Label>
            <Label></Label>
            <Label></Label>
            <Label></Label>
            <Label></Label>
        </StackPanel>
    </StackPanel>
</Page>
